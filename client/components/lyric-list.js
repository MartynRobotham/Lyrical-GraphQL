import React from 'react';
import {graphql} from "react-apollo";
import {getLyrics} from "../queries/lyrical-queries";

const LyricList = (props) => {
    return (
        <div>

        </div>
    );
};

export default LyricList;
// export default graphql(getLyrics)(LyricList);
